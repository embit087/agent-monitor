export function NotepadWindow() {
  return (
    <div className="notepad-window">
      <p style={{ padding: 20, color: 'var(--text-secondary)' }}>
        Notepad — Monaco editor will be integrated here.
      </p>
    </div>
  )
}
